# 三月七桌宠更新文档

更新日期：2026-07-24
适用仓库：`MihYux/desktop-march-7th`
更新范围：本次对话中新增、修改和验证的功能

## 1. 更新概述

本次更新将原有的“三月七自由聊天桌宠 + 本地发行沙盒”扩展为一套受控的角色化游戏发行原型。

系统现在包含两个独立界面：

- 玩家桌宠：负责日常聊天、长期关系记忆、通信中心和最终获准内容展示。
- 角色发行控制台：负责发行方案导入、事实锁定、知识审核、候选生成、人工审批、灰度发布、投递和紧急停止。

AI 不会直接修改发行策略或自行向玩家发送内容。模型只负责生成候选文案，候选必须经过确定性检查、人工审批和不可变内容包发布，才能进入玩家通信中心。

## 2. 受控长期记忆

### 2.1 新增记忆候选机制

聊天中出现高置信度、非敏感的明确表达时，系统可以生成一张简短记忆候选卡片，例如：

- 玩家希望使用的称呼。
- 玩家明确表达的偏好。
- 玩家主动描述的互动习惯。
- 玩家与三月七之间明确发生的共同经历。

候选不会直接成为长期记忆。玩家必须点击确认后，系统才会保存。

### 2.2 记忆记录扩展

`MemoryRecord` 新增或补充了以下字段：

- `category`
- `sourceType`
- `sourceId`
- `status`
- `consentAt`
- `lastUsedAt`
- `campaignReusable`
- `tags`
- `memoryVersion`
- `rationale`

记忆状态支持：

- `candidate`
- `confirmed`
- `rejected`
- `deleted`

### 2.3 敏感记忆拦截

以下内容不会形成长期记忆候选：

- 医疗和健康敏感信息。
- 财务和银行卡信息。
- 账号、密码和凭据。
- 精确地址和位置。
- 电话、邮箱等联系方式。
- 未成年人敏感信息。

### 2.4 记忆使用规则

- 普通聊天最多使用三条已确认且相关的记忆。
- 发行内容最多引用一条记忆。
- 发行引用需要玩家单独开启 `campaignReusable`。
- 关闭记忆、关闭普通引用或撤销发行引用后，后续上下文立即停止使用。
- 系统不会永久保存完整聊天原文作为长期记忆。

### 2.5 玩家侧 UI

玩家聊天窗口新增记忆候选卡片，可选择：

- 不保存。
- 让三月七记住。

相册/记忆管理界面新增“允许发行内容引用”开关，与普通聊天引用授权分离。

## 3. Prompt、RAG 与模型候选生成

### 3.1 职责分离

发行模型上下文拆分为：

- Persona Prompt：角色身份、语气、人际边界和安全边界。
- Skill Profile：三月七可做和不可做的行为规则。
- Fixed Facts：版本名称、时间、奖励、产品入口等人工锁定事实。
- Approved Knowledge：经人工审核、处于有效范围内的发行知识片段。

完整发行方案不会长期写入系统 Prompt。

### 3.2 文档导入

发行控制台支持：

- DOCX
- PDF
- TXT
- Markdown
- 直接粘贴文本

导入后会生成带来源信息的知识片段。PDF 片段保留来源页码，便于审核和追溯。

新增依赖：

- `mammoth`
- `pdf-parse`

### 3.3 知识审核与检索

知识片段默认处于未批准状态。发行人员可以逐条批准或撤销。

检索前会按以下条件过滤：

- 当前活动。
- 当前发行阶段。
- 玩家地区。
- 玩家分群。
- 解禁时间。
- 失效时间。
- 人工批准状态。

每次模型生成最多检索六条相关知识片段。

### 3.4 受限模型生成

新增 `limited_generation` 模式。该模式使用已配置的 DeepSeek 生成结构化候选：

```ts
interface CampaignGenerationResult {
  title: string;
  body: string;
  actionId?: string;
  usedFactIds: string[];
  usedKnowledgeChunkIds: string[];
  optionalMemorySlot?: string;
  riskFlags: string[];
}
```

模型只能使用当前上下文中的锁定事实和已审核知识。资料不足时必须返回风险标记，不能自行补写。

### 3.5 来源和新增事实检查

系统会检查模型返回的：

- 日期和时间。
- 奖励数值。
- 概率和百分比。
- 金额和次数。
- 活动入口。
- 专有名词。

如果候选中的结构化声明不能在它引用的事实或知识片段中逐字找到，生成会失败或自动审核不通过。

## 4. 强制约束层

### 4.1 生成前检查

生成前检查：

- 活动是否已人工批准并处于运行状态。
- 全局 Kill Switch 是否关闭。
- 固定事实是否锁定。
- 事实和知识是否已解禁、未失效。
- 地区和分群是否适用。
- 玩家联系策略是否允许。

### 4.2 生成后检查

候选生成后检查：

- 事实来源是否合法。
- 是否新增日期、奖励或数值声明。
- 是否存在外部链接。
- 是否存在多个 CTA。
- 是否出现 OOC、客服腔或营销机器人表达。
- 是否包含诱导充值、虚假稀缺或收益保证。
- 是否包含内疚、排他或依赖操纵。
- 是否泄露内部代号、系统规则或隐私信息。
- 是否引用未授权记忆。
- 文案长度是否符合限制。

任一检查失败后，内容保持未发送状态，不会自动让模型修改后重试投递。

### 4.3 投递前检查

投递前重新检查：

- 活动运行状态。
- 有效且未撤销的内容包。
- Kill Switch。
- 玩家退订和内容类型授权。
- 勿扰时间。
- 七天安静期。
- 每周主动联系上限。
- 版本内容每周一次上限。
- 两次主动联系至少间隔 24 小时。
- 七天内模板去重。
- 负面情绪、聊天中或操作其他面板等不适合触达的场景。

## 5. 主动联系策略

联系策略新增或强化了以下规则：

- 所有主动联系默认每周最多两次。
- 版本内容每周最多一次。
- 任意两次主动联系至少间隔 24 小时。
- 相同模板七天内不重复。
- 连续忽略两次后进入七天安静期。
- “不感兴趣”停止当前内容类型。
- “不再接收版本内容”停止全部版本活动。
- 暂停同行、关闭主动联系或进入勿扰时不触达。
- 负面情绪、聊天中、面板操作中或没有自然触发条件时保持沉默。

## 6. 独立角色发行控制台

### 6.1 独立运行界面

新增独立的 Operator Renderer 和安全 Preload：

- `src/operator/OperatorApp.tsx`
- `src/operator/api.ts`
- `electron/operator-preload.cjs`

玩家 Preload 不再暴露发行任务创建、审核、发布和投递能力。

### 6.2 控制台功能

控制台支持：

- 新建和编辑发行任务。
- 固定事实录入和锁定。
- DOCX、PDF 和文本方案导入。
- 知识片段审核。
- 任务自动检查。
- 任务人工审批。
- 启动、暂停、恢复、停止和完成活动。
- 按发行阶段生成候选内容。
- 消息自动审核。
- 消息人工审批。
- 发布不可变内容包。
- 灰度范围选择。
- 投递前策略检查。
- 全局紧急停止。

### 6.3 不可变 CampaignBundle

人工批准的内容会发布为带版本号和 SHA-256 校验值的 `CampaignBundle`。

内容包包含：

- 活动 ID。
- Bundle 版本号。
- 发布时间和发布者。
- 灰度比例。
- 固定事实 ID。
- 知识片段 ID。
- 消息 ID。
- 校验值。
- 激活或撤销状态。

发布新内容包时，旧的有效内容包会被撤销。

### 6.4 灰度发布

控制台支持：

- 5%
- 25%
- 100%

玩家是否进入灰度通过稳定哈希确定，同一玩家和活动的分桶结果保持一致。

本地必达测试建议选择 100%。

### 6.5 Kill Switch

全局 Kill Switch 开启后会：

- 停止未结束活动。
- 撤销有效内容包。
- 使未发送版本消息失效。
- 清理活动关联。
- 阻止新的内容包发布和投递。

## 7. 玩家端与内部数据隔离

新增玩家只读数据投影：

- 玩家端不获取原始固定事实。
- 玩家端不获取知识来源和知识片段。
- 玩家端不获取任务自动审核和人工审核详情。
- 玩家端不获取未激活或不属于自身灰度范围的内容包。
- 内部审核和风险日志不会进入玩家数据。

控制台和桌宠可以在同一个 Electron 进程中运行，避免两个独立进程同时写入同一份本地数据。

## 8. 运行方式

玩家桌宠：

```bash
npm run start
```

独立发行控制台：

```bash
npm run operator
```

同时运行桌宠与发行控制台：

```bash
npm run all
```

`npm run all` 使用 `MARCH7TH_SURFACE=all` 在同一 Electron 进程中创建两个窗口。

## 9. 控制台 UI/UX 优化

发行控制台已从桌宠小窗规格改为独立桌面工作台布局。

主要调整：

- 默认窗口扩大至 `1400 × 900`。
- 最小窗口尺寸调整为 `1080 × 700`。
- 兼容 Windows DPI 和可用工作区。
- 主标题提升至 34px。
- 正文和表单字号提升至 13–16px。
- 按钮高度提升至约 43–48px。
- 左侧任务列表扩宽至约 290px。
- 增加五阶段工作流导航：
  1. 任务事实
  2. 方案知识
  3. 任务审核
  4. 内容生成
  5. 发布投递
- 扩大表单、卡片、审核结果和知识正文间距。
- 知识片段区域增加可读高度。
- 排期卡片改为宽屏双列、窄屏单列。
- 消息预览提升标题、正文和来源信息字号。
- 增加 1300px 和 1120px 响应式布局。
- 统一焦点态、按钮层级、状态色和卡片阴影。

## 10. 桌宠卡顿修复

### 10.1 问题原因

Windows 透明置顶窗口中，大尺寸角色 PNG 的无限浮动和呼吸动画会让 Chromium GPU 合成进程持续高负载，导致桌宠看起来卡住。

### 10.2 修复内容

- Windows 下关闭高负载硬件合成。
- 移除角色永久循环浮动动画。
- “观察”和“休息”状态改为静态视觉变化。
- 保留鼠标悬停和点击时的短时反馈动画。
- 保留 DeepSeek 45 秒请求超时，避免网络请求无限等待。

修复后，两秒 CPU 采样中所有 Electron 进程的 CPU 增量均为 0，窗口保持响应。

## 11. 主要新增文件

- `electron/campaign-generator.cjs`
- `electron/memory-candidates.cjs`
- `electron/operator-preload.cjs`
- `electron/release-knowledge.cjs`
- `electron/release-system.test.cjs`
- `electron/release-constraints.test.cjs`
- `src/operator/OperatorApp.tsx`
- `src/operator/api.ts`

## 12. 主要修改文件

- `electron/main.cjs`
- `electron/preload.cjs`
- `electron/companion-store.cjs`
- `electron/campaign-review.cjs`
- `electron/contact-policy.cjs`
- `electron/companion-store.test.cjs`
- `electron/campaign-review.test.cjs`
- `electron/prd-acceptance.test.cjs`
- `src/App.tsx`
- `src/ai/types.ts`
- `src/domain/types.ts`
- `src/main.tsx`
- `src/vite-env.d.ts`
- `src/components/AlbumPanel.tsx`
- `src/components/CampaignStudioPanel.tsx`
- `src/styles.css`
- `package.json`
- `package-lock.json`
- `README.md`
- `docs/PRIVACY.md`
- `CHANGELOG.md`

## 13. 测试与验收

本次更新完成后通过：

- 84/84 自动化测试。
- TypeScript 构建。
- Vite 生产构建。
- 36/36 PRD 原型验收。
- 发布审计。
- 依赖安全审计，已知依赖漏洞为 0。

新增测试覆盖：

- DOCX 导入。
- PDF 导入和页码追溯。
- 文本方案切分。
- 知识批准和解禁。
- 地区、分群和阶段过滤。
- 模型结构化输出。
- 模型新增日期和奖励拦截。
- 记忆候选和敏感信息拦截。
- 记忆确认与发行独立授权。
- 玩家数据与内部审核数据隔离。
- Operator Preload 权限边界。
- Kill Switch。
- 24 小时冷却。
- 每周版本内容上限。
- 负面或忙碌场景抑制。
- 5%、25%、100% 灰度分桶。
- Windows 透明窗口高负载防护。

## 14. 当前边界和后续工作

当前实现仍是本地桌面原型，尚未完成正式线上发行基础设施。

正式发行前仍需要：

- 将本地只读 IPC 内容通道替换或扩展为正式服务端内容接口。
- 接入真实玩家分群、地区和行为信号。
- 建立线上灰度观测、指标面板和自动回滚机制。
- 完成角色视觉素材的正式公开和商业分发授权归档。
- 完成 Windows/macOS 签名、公证和安装包验证。
- 建立正式自动更新源和回滚流程。
- 对 DeepSeek、DashScope 和数据保留规则进行上线前合规复核。

这些事项属于正式发行门槛，不影响当前本地原型的功能验证。
