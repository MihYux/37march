# 三月七角色发行系统合并指南

本仓库保存的是角色发行系统功能包，不包含原项目的全部历史和无关文件。

功能说明请先阅读：

- [`更新文档.md`](./更新文档.md)

## 推荐方式：应用 Git 补丁

将目标版本切换到准备接收新功能的分支，并确保工作区干净：

```bash
git status
git switch -c feature/character-release-system
```

然后应用 `patches` 目录中的补丁：

```bash
git am patches/0001-Add-controlled-character-release-system.patch
```

如果目标版本与原始基线差异较大，`git am` 可能出现冲突。解决冲突后执行：

```bash
git add <已解决的文件>
git am --continue
```

如需取消本次应用：

```bash
git am --abort
```

## 备选方式：按文件迁移

`feature-files` 目录保存本次提交涉及文件的完整版本，并保持原仓库目录结构。

可以按以下模块分批迁移：

### 1. 记忆系统

- `electron/memory-candidates.cjs`
- `electron/companion-store.cjs`
- `src/App.tsx`
- `src/components/AlbumPanel.tsx`
- `src/ai/types.ts`
- `src/domain/types.ts`

### 2. 发行知识与模型生成

- `electron/release-knowledge.cjs`
- `electron/campaign-generator.cjs`
- `electron/campaign-review.cjs`
- `electron/contact-policy.cjs`

### 3. 独立发行控制台

- `electron/operator-preload.cjs`
- `src/operator/OperatorApp.tsx`
- `src/operator/api.ts`
- `src/components/CampaignStudioPanel.tsx`
- `src/main.tsx`
- `src/styles.css`

### 4. Electron 接入与权限隔离

- `electron/main.cjs`
- `electron/preload.cjs`
- `src/vite-env.d.ts`

### 5. 依赖和脚本

- `package.json`
- `package-lock.json`

### 6. 测试

- `electron/release-system.test.cjs`
- `electron/release-constraints.test.cjs`
- `electron/campaign-review.test.cjs`
- `electron/companion-store.test.cjs`
- `electron/prd-acceptance.test.cjs`

## 合并后的验证

安装依赖：

```bash
npm install
```

运行完整检查：

```bash
npm run check
```

预期结果：

- 自动化测试全部通过。
- TypeScript 和 Vite 构建通过。
- PRD 原型验收通过。
- 发布审计通过。

同时启动桌宠和发行控制台：

```bash
npm run all
```

## 合并注意事项

- `electron/companion-store.cjs` 和 `electron/main.cjs` 改动较大，跨版本迁移时优先人工检查冲突。
- 不要用旧版 `electron/preload.cjs` 覆盖新版权限收缩，否则玩家端可能重新获得内部发行操作接口。
- `package.json` 和 `package-lock.json` 必须同步迁移，以安装 DOCX/PDF 解析依赖。
- `src/domain/types.ts`、`src/vite-env.d.ts` 和 Operator API 需要保持一致。
- 正式上线前仍需接入真实服务端内容接口、玩家分群、线上灰度观测和回滚机制。
